window.env = window.env || {};

window.env.app = {
    actions: {
        'login-actions/authenticate': 'login',
        'register-actions/register': 'register',
        'register-actions/reset-password': 'reset-password'
    },
};
